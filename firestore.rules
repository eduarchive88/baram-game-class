rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 모든 접근은 인증된 사용자(익명 포함)만 가능하도록 제한
      allow read, write: if request.auth != null;
    }
  }
}
